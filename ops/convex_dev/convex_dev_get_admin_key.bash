#!/usr/bin/env bash
set -x # Print all executed commands to the terminal
set -e # Exit immediately if a command exits with a non-zero status

cat ~/.convex/anonymous-convex-backend-state/anonymous-dcc-app/config.json
